import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SnakeGame = () => {
  return (
    <View>
      <Text style={{color:'red'}}>SnakeGame</Text>
    </View>
  )
}

export default SnakeGame

const styles = StyleSheet.create({})